# OmniCode Stabilization Test Matrix

Last updated: 2026-09-21

Results are changed to **Pass** only after the recorded behavior was observed.
“Covered” means an existing automated test exercises the code path; it does not
substitute for a real integration test where one is required.

| System | Feature | Test | Result | Automated/Manual | Notes |
| --- | --- | --- | --- | --- | --- |
| Startup | Fresh profile | Launch packaged app into setup | Pass | Automated smoke + three-launch soak | Refreshed Intel Sonoma package rendered and validated the six-step onboarding on the first run, advanced to the workbench, and logged no renderer exception |
| Startup | Workbench | Complete setup and render main window | Pass | Automated smoke | No white/infinite loading state |
| Startup | Window lifecycle | Minimize, zoom/full-screen, close, reopen | Pass | Packaged macOS E2E | Real resize, minimize/restore, Zoom/unzoom, full-screen enter/exit, close, renderer teardown, `open -a` reopen, and renderer recreation passed |
| Startup | Menu | Open each native menu and invoke core commands | Pass | Packaged macOS E2E | All 11 app menus opened, 48 expected items verified, representative renderer command invoked twice; core accelerator commands are covered by packaged shortcut audits |
| Startup | Early command delivery | Preserve menu/Finder commands before React subscribes | Pass | Unit + production build | Preload subscribes before renderer paint and drains a bounded ordered queue; ordering, live delivery/unsubscribe, and two-item queue bounds passed |
| Startup | Console | Capture renderer/unhandled startup errors | Pass | Automated smoke | No meaningful errors captured |
| Filesystem | Open folder | Authorize and display real temporary workspace | Pass | Automated smoke | Tree returned two entries |
| Filesystem | Native folder dialog | Open, cancel, authorize, and restore exact folder | Pass | Packaged macOS E2E | File → Open Folder showed the real Open/Cancel/New Folder sheet, opened a disposable exact folder, Cancel left it unchanged, and a second native selection restored the original workspace |
| Filesystem | Open file | Open real file from cold launch | Pass | Packaged E2E + smoke | Cold launch and real `⌘O` native sheet loaded exact disposable file content into offline Monaco |
| Filesystem | Create file/folder | Verify nested real filesystem changes | Pass | Packaged E2E + integration | Actual Explorer context-menu controls and styled dialogs created an exact Unicode/space folder and file; nested backend path also passed |
| Filesystem | Rename file/folder | Spaces, punctuation, Unicode | Pass | Packaged E2E + unit | Actual Explorer context-menu Rename remapped the open tab and exact real path; collision/scope rules are unit-covered |
| Filesystem | Move file/folder | Nested paths and invalid cycles | Pass | Packaged integration + unit | Real nested file moved; invalid cycles covered by unit test |
| Filesystem | Duplicate file/folder | Verify contents and destination | Pass | Packaged E2E + unit | Actual Explorer Duplicate created the expected sibling and exact source bytes on disk |
| Filesystem | Trash file/folder | Verify scoped deletion | Pass | Packaged macOS E2E + unit | Actual Explorer Move to Trash and native confirmation removed duplicate/original files plus created/parent folders; four dialogs and exact disk state passed |
| Filesystem | Save | Persist editor content and clear dirty state | Pass | Packaged E2E | Monaco edit saved and exact disk bytes verified |
| Filesystem | Save As | Persist to user-selected path | Pass | Packaged macOS E2E | Real `⌘⇧S` sheet wrote exact bytes under a space-containing name, retained source, and updated the active tab path |
| Filesystem | Autosave | Delayed write and conflict behavior | Pass | Packaged E2E | Real Monaco edit auto-wrote exact bytes after the delay; dirty state cleared only after success; conflicting external revision was preserved |
| Filesystem | External changes | Reload/close/conflict behavior | Pass | Packaged E2E | Clean write reloaded, clean delete closed its tab, and dirty competing write blocked Save without losing either version |
| Filesystem | Recent workspaces | Persist/reopen and remove missing entries | Pass | Unit + packaged restart E2E | App restarted without a launch path, rendered the exact persisted recent folder, reopened it from the Welcome UI, loaded its real 10-entry tree, and quit cleanly with zero renderer errors |
| Filesystem | New Project | Name and native destination create exact folder | Pass | Packaged macOS E2E | Styled dialog plus native destination sheet created and opened `Project Ω Audit` on the real filesystem |
| Filesystem | Outside workspace | Reject unauthorized read/write | Pass | Packaged integration + unit | `/etc/passwd` read rejected through production preload/backend |
| Filesystem | Drag/drop | File and directory authorization | Pass | Packaged E2E | Chromium's OS drag path opened exact external Unicode file/content, authorized an external folder as workspace, moved a real Explorer file into a folder, restored the original workspace, and cleaned up |
| Filesystem | Reveal/Open With | Finder integration | Pass | Packaged macOS E2E | Explorer actions selected the exact file in Finder and chose TextEdit through the native Open With sheet; exact document/content opened |
| Editor | Multiple tabs | Open, switch, and close files | Pass | Packaged E2E | Three language tabs opened via Quick Open, switched, and closed with native `⌘W` |
| Editor | Languages | Map HTML/CSS/JS/TS/Python/Java/C/C++/Swift/Rust/Go/JSON/Markdown | Pass | Unit | Seven mapping/registry tests |
| Editor | Core editing | Type, selection, copy/paste, undo/redo | Pass | Packaged macOS E2E | System clipboard copy/paste, select-all, insertion, model-level native Undo/Redo, and exact Save bytes passed; the user's prior clipboard was restored without logging it |
| Editor | Find/replace | Find, replace, find-all | Pass | Packaged E2E | Native `⌘F` opened Monaco Find; real inline Replace All changed and saved exact bytes; workspace search/multi-file Replace All also passed |
| Editor | Visual features | Lines, indentation, brackets, folding, minimap | Pass | Packaged E2E | Auto-indent/bracket completion, line numbers, syntax tokens, folding control, minimap, and TypeScript state rendered |
| Editor | Dirty state | Indicator, save, close prompt | Pass | Packaged macOS E2E | Dirty/clean transitions and tab Cancel/Discard passed; native window sheet Cancel retained buffer, Discard preserved disk, and Save All wrote exact bytes before closing |
| Editor | Resize | Editor relayout after panel/window changes | Pass | Packaged E2E | Explorer, AI, and bottom-panel min/max pointer drags committed without renderer errors |
| Shortcuts | File/edit | ⌘S, ⌘O, ⌘F, ⌘W, ⌘Z, ⌘⇧Z | Pass | Packaged macOS E2E | All listed native accelerators passed; `⌘O` selected an exact path through the real macOS sheet |
| Shortcuts | Navigation | ⌘P and ⌘⇧P | Pass | Packaged E2E | Real macOS System Events keystrokes opened Quick Open and Command Palette |
| Shortcuts | Settings/terminal/AI | ⌘,, ⌘`, ⌃⇧`, ⌘I | Pass | Packaged macOS E2E | Settings, terminal toggle, new-terminal creation, and selected-code Inline AI all passed through real System Events keystrokes |
| Search | Workspace search | ripgrep and fallback results | Pass | Packaged E2E + unit | Real Search sidebar returned two exact results; unit fallback remains covered |
| Search | Replace all | Scoped replacement | Pass | Packaged E2E + unit | Confirmed UI operation changed both real files and retained its success notice |
| Search | Ignore/include/exclude | `.gitignore`, `.omnicodeignore`, globs | Pass | Unit + packaged soak | A 1,203-file non-Git fixture returned exactly one valid match; ignored folders and explicit exclude stayed absent after fixing ripgrep ordering and `--no-require-git` |
| Terminal | zsh | Start real login shell | Pass | Packaged smoke | `/bin/zsh` launched |
| Terminal | Working directory | `pwd` equals workspace | Pass | Packaged integration | Real PTY printed exact authorized workspace cwd |
| Terminal | Basic commands | `ls`, `echo`, `whoami`, environment | Pass | Packaged integration | Real zsh identity and environment markers passed |
| Terminal | PATH | Intel/Apple Homebrew paths visible | Pass | Packaged integration + unit | Both standard Homebrew bin paths and real Node/Git lookup passed |
| Terminal | Interactive | Ctrl+C, history, interactive process | Pass | Packaged E2E + integration | Real sleep interrupted; zsh Up-arrow history replayed the prior marker command and produced output again |
| Terminal | Sessions | Create/switch/rename/close/restart/clear multiple terminals | Pass | Packaged E2E + integration | Toolbar and native shortcut created sessions; tabs switched, split rendered, styled rename changed the active label to `Audit Terminal`, and search/clear/restart/kill/close reflected real state |
| Terminal | Resize/scroll/copy/paste | xterm behavior | Pass | Packaged macOS E2E | PTY resize, xterm mount, 1,520-row scrollback navigation, search selection, clear, and exact system clipboard copy/paste passed; prior clipboard restored |
| Runtime | Detection | 33 allowlisted tools return installed/missing states | Pass | Packaged integration + unit | Requested tool states matched independent host command lookup |
| Runtime | Missing tools | Clean state without crash or stack trace | Pass | Automated | Runtime tests and setup smoke |
| Runtime | Fresh Mac shims | Detection does not trigger Apple installer | Pass | Unit | Regression coverage |
| Runtime | Installation | Formula/native paths, progress, cancellation | Pass | Unit/integration-mock | No real system changes made |
| Runtime | Native authorization/failure | Cancel from Setup before process and report real package-manager failure | Pass | Packaged macOS E2E | Command Palette → Setup → Programming Runtimes exposed the actual `Install Go` control; native Cancel started no installer/progress, returned to workbench, and left Go absent. Invalid ID was rejected pre-dialog; a separate real Go attempt reached Homebrew, failed on non-writable directories, stayed absent, and left no process or fake success |
| Hardware | Current Mac | Architecture/CPU/RAM/macOS/Metal correctness | Pass | Packaged integration | x64 Sonoma 14.8.9, Intel i5, 8 GiB, UHD 617, Metal 3 verified |
| Hardware | Recommendations | Memory-based model ranking | Pass | Unit | Real host comparison pending |
| Run | Python | Real success and syntax error through OmniCode | Pass | Packaged integration | `PYTHON_OK`; syntax error returned nonzero |
| Run | JavaScript | Real success and runtime error through OmniCode | Pass | Packaged integration | `NODE_OK`; thrown error returned nonzero |
| Run | C/C++ | Real compile/run and compiler error | Pass | Packaged integration | Clang and Clang++ outputs passed; invalid C returned compiler diagnostics |
| Run | Swift | Real script/compile | Pass | Packaged integration | `SWIFT_OK`; first cold invocation was slow but completed |
| Run | Java | Real compile/run | Pass | Packaged integration | javac/java produced `JAVA_OK` |
| Run | Rust | Real Cargo build/run | Blocked | Host integration | BLOCKED — USER CONFIGURATION REQUIRED: rustc/Cargo not installed |
| Run | Go | Real build/run | Blocked | Host integration | BLOCKED — USER CONFIGURATION REQUIRED: Go not installed |
| Run | Error reporting | stdout, stderr, exit code, missing tool | Pass | Packaged E2E + unit | Run UI showed stdout and `[Process exited with code 0]`; deliberate failures nonzero |
| Run | Custom configuration | Execute preRun/build/run/postRun with cwd/env | Pass | Packaged E2E | `.omnicode/settings.json` overrode the automatic JavaScript recipe; all four commands, space-containing cwd, inherited custom environment, and exit code 0 were exact; fixture cleaned up |
| Run | Captured Run Log | Accurately label and mirror real task output | Pass | Packaged E2E | The relabeled Run Log displayed exact pre/build/run/post markers and `[Process exited with code 0]`; OmniCode does not claim a debugger adapter |
| npm | Script detection | Read package scripts and choose runner | Pass | Packaged integration + unit | Disposable manifest exposed real `npm run` commands |
| npm | Install/run/server | Real lifecycle with approval boundary | Pass | Packaged E2E | `npm run verify` and dependency-free server passed; no `node_modules` or automatic install was created |
| Server | Static start | Start valid localhost port | Pass | Packaged E2E | Auto port returned reachable HTTP server and Run UI reflected state |
| Server | Assets/nested paths | HTML/CSS/JS/relative resources | Pass | Packaged integration | Real HTML, CSS, JS, and nested file bytes served successfully |
| Server | Secret denial | Deny `.git` and `.env` | Pass | Packaged smoke | Both returned 403 |
| Server | Stop/restart/port release | Lifecycle correctness | Pass | Packaged integration + unit | Static/package restart passed, stopped ports rebound, explicit conflict was useful |
| Server | Live reload | HTML/CSS/JS browser update | Pass | Packaged app + real browser | A repeatable audit opened Chrome at OmniCode's real localhost URL; editing the served HTML automatically changed the same tab from the unique before title to the unique after title; stop released the selected port (latest run 55978) |
| Git | Repository detection/status | Real disposable repository | Pass | Packaged E2E + unit | UI initialized repository and reflected clean/changed/staged states |
| Git | Stage/unstage/commit/diff | Actual state matches UI | Pass | Packaged E2E | Real working diff reached Output; UI stage/unstage/commit matched disk and Git |
| Git | Init/branches | Initialize, create, switch, delete | Pass | Packaged E2E | All branch controls passed; repaired styled dialogs created `audit-input-dialog-branch` and deleted the offered inactive branch in a disposable clone |
| Git | Fetch/pull/push | Report exit success/failure accurately | Pass | Packaged E2E + integration | Real local bare remote synchronized; missing-remote push rejected usefully |
| Git | Clone | URL dialog, native destination, validation, and actual clone | Pass | Packaged macOS E2E + integration + unit | Welcome UI cloned a real local bare remote through the repaired input dialog and native destination sheet, opened the result, and matched origin; public GitHub HTTPS clone passed via host Git |
| Dialogs | Text input and confirmation | Accept/cancel/keyboard/accessibility behavior | Pass | Packaged E2E | Clone dialog had role/name and Escape settled without a picker; clone/project/branch/terminal inputs completed their real operations; native browser confirmation was observed and Cancel returned false |
| GitHub | Authentication | Existing SSH/HTTPS/gh state | Blocked | Host inspection | `gh` absent and no safe configured writable remote; USER CONFIGURATION REQUIRED for authenticated writes |
| Keychain | Save/read/restart | All provider keys across manager instances | Pass | Native + packaged E2E | Isolated native test passed all providers; packaged temporary OpenAI key survived a full app restart and was actually used; user Google key untouched |
| Keychain | Update/remove | Replace and delete all provider keys | Pass | Native integration | Disposable keychain |
| Keychain | Secret leakage | Source/profile/config/Git/visible-diagnostic scan | Pass | Packaged automated audit | Final scan covered 143 repository, 30 isolated-profile, and 10 workspace files plus renderer local storage and visible diagnostics; zero unexpected credential-shaped matches, one known split synthetic redaction fixture, and no credential value was read/printed |
| OpenAI | Request adapter | Auth header, payload, response/errors | Pass | Unit | Mock server/fetch |
| OpenAI | Invalid credential | Stored key is used and an accurate redacted auth error reaches UI | Pass | Packaged E2E | Temporary audit key survived restart, produced authentication failure, never appeared in returned error, and was deleted |
| OpenAI | Real minimal successful request | Response reaches UI | Blocked | Manual/E2E | BLOCKED — USER CONFIGURATION REQUIRED; no valid OpenAI key is configured |
| Claude | Request adapter | Auth headers, payload, response/errors | Pass | Unit | Mock server/fetch |
| Claude | Real minimal request | Response reaches UI | Blocked | Manual/E2E | USER CONFIGURATION REQUIRED if no key |
| Gemini | Request adapter | Auth header, payload, response/errors | Pass | Unit | Mock server/fetch |
| Gemini | Real minimal request | Exact response reaches packaged app | Pass | Packaged E2E | Existing Keychain credential authenticated; exact `OmniCode Cloud AI Test Successful` response received; no renderer errors |
| Providers | Connection endpoints | Lightweight official endpoint, headers, auth/rate/network states | Pass | Unit | OpenAI, Anthropic, and Google request shapes and state mapping covered |
| Providers | Status UI | Stored vs authenticated/connected | Pass | Packaged E2E | Stored/not-tested after restart, Connected for valid Gemini, Authentication failed for invalid OpenAI; Save/Test/Delete exercised |
| Ollama | Installed/service states | Distinguish absent, stopped, available | Pass | Unit + packaged E2E | CLI/app absent and port 11434 closed on host; packaged API/UI accurately reported not installed; stopped/available branches unit-covered |
| Ollama | Missing-service chat error | Fail accurately without a fake response or raw stack | Pass | Packaged E2E + unit | Rebuilt app reports installation guidance; installed-but-stopped and HTTP/model error variants covered |
| Ollama | Model list | Real installed/running models | Blocked | Integration | Ollama service required |
| Ollama | Pull/cancel/delete/default | Real small-model lifecycle | Blocked | E2E | Ollama service/model required |
| Ollama | Real inference | Exact response and coding question | Blocked | E2E | Ollama service/model required |
| AI chat | Provider/model switching | Defaults and manual changes stay coherent | Pass | Renderer unit | Five regression tests |
| AI chat | Conversation clear/errors | UI resets and surfaces failure | Pass | Packaged live E2E + unit | Real Gemini responses reached UI; New Chat removed all messages; invalid auth and transient provider 503 produced accurate visible errors |
| AI chat | Markdown/code blocks | Render assistant response appropriately | Pass | Unit + packaged live E2E | Real Gemini response rendered an `h2`, list items, inline code, and a language-tagged fenced JavaScript block; raw HTML stays inert and no legacy assistant `<pre>` remains |
| Code AI chat | Streaming/stop | Generation updates and cancellation | Not implemented | Inspection | Code Chat remains request/response; Work Mode has verified streaming and Stop |
| Context | Current/open/selected files | Inspect exact outbound request | Pass | Packaged live E2E | Consent listed exact current/open/native-picked paths and 22-character selection; Gemini verified unique tokens from every category |
| Context | Workspace retrieval | Multi-file relevance and actual payload | Pass | Unit + packaged live E2E | Exact outbound system context inspected in unit test; real Gemini derived undisclosed TTL/table/prefix across five retrieved files after consent |
| Context | Terminal/problems/Git | Include only explicitly selected data | Pass | Packaged live E2E | Consent reported nonzero terminal/problems/Git payloads; Gemini verified terminal marker, real TypeScript diagnostic, and changed Git filename in one request |
| Indexer | Initial/changed/new/deleted/renamed | Maintain current index | Pass | Unit + packaged E2E | Watcher-driven update/create/rename/delete refresh passed; stale tokens and paths disappeared |
| Indexer | Ignore/binary/large | Exclude sensitive/binary/huge files | Pass | Unit + packaged soak | Indexed 1,203 allowed files in 1.53 s; ignored three directory roots and skipped the binary fixture while the renderer kept ticking |
| Inline AI | Proposal/accept/reject/save | Real selected-code workflow | Blocked | E2E | Working AI backend required |
| Agent | Inspect/plan/propose files | Disposable multi-file task | Pass | Packaged live E2E | Two transient 503 attempts stopped safely with no writes; later retry read multiple files, proposed one derived file, opened review, accepted, and undid it |
| Agent | Plan parsing/path safety | Reject traversal, malformed, incomplete, and oversized plans | Pass | Unit | Relative paths only; 50-file, 20-step, and 10-command limits verified |
| Agent | Command permission | Approval and backend enforcement | Pass | Unit + packaged macOS E2E | Main process blocked unsafe/wrong-workspace calls; native Cancel returned false with no file, native Run Command returned true, and only the approved safe command executed through a workspace PTY |
| Agent | Model-generated command UI | Model plan → Review & Run → native approval → PTY | Pass | Packaged live E2E | After three transient retries, Gemini returned one command/no changes; Review & Run opened the exact native sheet, approval executed in the workspace PTY with exit 0/exact bytes, marker was removed, prior provider/model and confirmation behavior were restored, and renderer errors were zero |
| Diff | File/hunk accept/reject/all | Filesystem matches decisions | Pass | Unit + packaged E2E | Real three-file transaction stayed staged until accept; reject preserved disk |
| Diff | Create/delete/undo | Restore exact filesystem state | Pass | Unit + packaged E2E | Modify/create/delete acceptance and full snapshot undo matched disk |
| Settings | Workspace JSON | Validate/read/write and corrupt input | Pass | Unit + packaged E2E | Valid round-trip, atomic mode `0600`, invalid-port no-overwrite, malformed JSON error, restore, and cleanup passed |
| Settings | User preferences | Theme/autosave/permission restart | Pass | Packaged E2E | Theme, autosave, permission, autocomplete enablement/provider/model all survived full app restart; audit snapshot restored |
| Settings | Corrupt renderer preferences | Safe fallbacks instead of unsupported states | Pass | Unit | Invalid theme→system, permission→ask, provider→Ollama, malformed/oversized model→empty |
| Settings | AI model/default | Persist selected/default local model | Blocked | Unit + host inspection | Preference validation/persistence is covered; real selection/restart requires an installed Ollama model |
| Themes | Dark/light/system | Contrast and component states | Pass | Packaged E2E | All modes switched; light ratios muted 4.72/accent 4.51/warning 4.70, dark 5.42/5.08/7.17; 2px focus and 0.45 disabled states passed |
| Layout | Resizing | Sidebars/panel/window min/max | Pass | Packaged E2E | Sidebar 200–480, AI 300–640, panel 140–60% viewport; hide/show controls and editor relayout passed |
| Layout | Persistence | Restore panel dimensions | Not implemented | Inspection | No persistence code found |
| macOS | Notifications | Validate request and display native banner | Blocked | Packaged E2E + platform inspection | Valid request accepted and three malformed/oversized requests rejected; no banner appeared because Electron documents that unsigned macOS development builds are not delivered to Notification Center — APPLE DEVELOPER ID REQUIRED |
| Security | Renderer sandbox/IPC sender | Reject untrusted calls/navigation | Pass | Automated/smoke + inspection | Sender must be the current window's main frame at the exact packaged renderer URL; renderer is sandboxed with no Node access, external main-frame navigation was blocked in the packaged smoke, and new windows are denied |
| Security | Workspace boundary | Reject arbitrary external paths | Pass | Unit + packaged E2E | Workspace file access, terminal cwd, AI attachments, Agent roots, and diff paths outside the current authorized workspace were rejected |
| Security | Dangerous commands | Backend approval enforcement | Pass | Unit + packaged macOS E2E | Main-process policy blocks privileged/destructive/Keychain/nested-shell patterns; native Cancel prevented execution and native approval allowed one exact safe workspace command |
| Diagnostics | Persistent structured log | Record categorized failures without secrets | Pass | Unit + packaged E2E | Real boundary failure recorded timestamp/subsystem/operation/permission category in a mode-`0600` log; IPC argument and authorization-header scans passed; rotation/redaction/symlink tests passed |
| Failure | Offline/provider/rate-limit/timeout | Graceful accurate errors | Partial | Unit + packaged E2E | Invalid auth, a real Gemini 503, and repeated live `fetch failed` transport errors surfaced honestly with no fake success; rate-limit/timeout paths pass unit tests, while a physically disconnected-network E2E remains |
| Failure | Permission/read-only/moved files | Graceful accurate errors | Pass | Packaged E2E + unit | Real mode-`0555` save surfaced EACCES guidance; moving a workspace during a dirty edit produced a visible conflict error; both cases preserved disk bytes and dirty buffers, then recovered cleanly |
| Performance | Startup/idle | Time, CPU, memory | Pass | Packaged three-launch soak | Fresh-profile shell/workspace 5.4/7.1 s; warm at most 4.3/4.7 s; settled CPU 1.4–2.6%, RSS 392–441 MiB, quit in 0.6–1.1 s with complete helper cleanup |
| Performance | Large workspace/index/search | Responsiveness and bounds | Pass | Packaged measurement | 1,203 files: workspace open 415 ms, index 1.53 s, search 47 ms; renderer stayed responsive and results/ignores were exact |
| Performance | Cleanup/leaks | Listeners, PTYs, servers, AI operations | Pass | Packaged soak | 8 PTYs and 3 servers created/stopped; server was not left running, app RSS settled 397→422 MiB, median idle CPU 0%, and no renderer error was captured |
| Production | Build | Typecheck, electron-vite, and native helper build | Pass | Automated | Current source typechecks; main/two preload bundles, 3,122 renderer modules, and the x86_64 Swift helper compile successfully |
| Production | Intel app | Launch and core smoke on Sonoma | Pass | Automated smoke | Current x64 directory package passed startup, workspace IPC, real zsh PTY, server/public-secret boundaries, blocked external navigation, packaged Omni helper/Accessibility readiness, and zero renderer errors; earlier Work/Gemini smokes remain valid |
| Production | Apple Silicon app | Correct executable/native slices | Pass | Automated inspection | Final 0.3.1 app executable and active `node-pty` module are arm64; the DMG verifies and matching-hardware launch remains blocked |
| Production | Archives | DMG/ZIP integrity and checksums | Pass | Automated | 0.3.1 Intel/Apple Silicon DMGs verify; both ZIPs decompress cleanly; embedded versions, original icons, claimed executable/native architectures, and fresh SHA-256 sums pass |
| Production | Signing/notarization | Gatekeeper-ready public release | Blocked | External | Developer ID certificate required |
| Modes | Separate Code and Work modes | Switch both ways without replacing Code workbench | Pass | Unit + packaged E2E | Work rendered as an independent surface; Code remained mounted and returned with its state boundary intact |
| Work conversations | CRUD/history | Create, reopen, rename, pin, search, and delete | Pass | Unit + packaged E2E | Real packaged conversation title/message persisted, search found it, and cleanup deleted it |
| Work conversations | Store safety | Atomic private persistence, validation, corruption recovery, bounds | Pass | Unit | Concurrent writes, mode `0600`, backups, IDs, message/conversation/store limits, and recovery are covered |
| Work messages | Response actions | Copy, edit user turn, regenerate, and retry without duplicate exchanges | Pass | Renderer unit + live E2E | Live Gemini copy/regenerate/edit-and-retry paths reused the same exchange IDs and preserved correct history |
| Work AI | Streaming | Forward provider deltas into the visible and persisted response | Pass | Unit + packaged live E2E | Gemini 3.7 returned the exact `OmniCode Packaged Work Test Successful` marker and streamed content matched the final response |
| Work AI | Stop generation | Cancel in-flight request and preserve an honest partial state | Pass | Unit + live E2E | Main request was sender-bound and aborted; partial response persisted as cancelled rather than completed |
| Work AI | Tool loop | Provider proposes registered tool, result returns, final answer renders | Pass | Unit + live E2E | Gemini 3.7 used Managed Browser on Example Domain; activity metadata recorded the successful real tool call |
| Work AI | Tool-call safety limits | Reject duplicate calls, unknown tools, bad inputs/results, over-limit calls and cancellation | Pass | Unit | Bounded 8-step/12-call loop, 64 KB input, 256 KB result, ID validation, redaction, and abort behavior covered |
| Work AI | Local model tool gate | Keep unknown/unsupported/uninstalled Ollama models chat-only | Pass | Unit + main-process inspection | Connected-app schemas are exposed only to an installed exact local model with explicit `toolUse: true` metadata |
| Models | Cloud catalog | Paginate/filter/cache OpenAI, Anthropic, and Gemini models | Pass | Unit + live integration | Auth, size/time/page bounds, stale cache, fallback and redaction covered; live Google returned 20 account-visible text models |
| Models | Work selector | Provider/model dropdown, refresh, capability and stale/error states | Pass | Renderer unit + packaged E2E | Work packaged surface rendered both selectors and used the chosen Gemini 3.7 model |
| Models | Code Chat selector | Replace manual cloud model ID with catalog dropdown | Pass | Renderer unit + live E2E | UI exposed no raw cloud model field; selecting Gemini 3.7 produced the exact Code selector verification response |
| Models | Settings autocomplete selector | Use provider catalog instead of raw cloud model text | Pass | Renderer unit + live UI | Google selector showed the same 20 live models and retained an explicit refresh control |
| Connectors | Registry/state | Register, connect, verify, refresh, disconnect and reject duplicates | Pass | Unit + packaged E2E | Managed Browser reached connected only after creating its isolated page, then returned to not-connected after cleanup |
| Tools | Registry/permissions | Validate mode/schema/scope/action/timeout/result and confirmation policy | Pass | Unit | Renderer cannot register executors; sensitive/destructive policy is enforced in the trusted process |
| Browser connector | Real public HTTPS page | Open/read/find on Example Domain | Pass | Packaged E2E | `browser.open`, `browser.read`, and `browser.find` were registered; real title and one matching excerpt returned |
| Browser connector | Isolation/network safety | Block HTTP, credentials, private/local addresses, redirects, popups, downloads, permissions | Pass | Unit + integration | Dedicated persistent partition is separate from normal browser state; unsafe destinations/actions are denied |
| Work attachments | Text/source import | Private copied attachment, opaque persisted metadata, bounded provider context | Pass | Unit + live E2E | Live Markdown hidden value reached Gemini; no original path persisted or reached the renderer conversation record |
| Work attachments | Unsafe/unsupported input | Reject traversal, symlink, binary, oversize, empty, and excessive attachments | Pass | Unit | Text/source limit is 2 MB per file and 400 KB total extracted context; errors are explicit |
| Work attachments | PDF/Word/spreadsheet/image extraction | Extract structured document/image content | Not implemented | Inspection | Formats requiring dedicated parsers are rejected honestly rather than sent as fake or binary text |
| Connected Apps | Settings management | Display only registered connectors and run real connect/disconnect | Pass | Renderer unit + packaged UI | Settings and Work Mode show one shared Google-account summary above the individual Gmail/Drive capabilities; no developer credential controls or placeholder connected services are exposed |
| Connected Apps | Google OAuth | Complete real shared-account authorization with the publisher client and combined Gmail/Drive scopes | Pass | Packaged manual | Registered developer Desktop client completed Google's system-browser consent with PKCE S256, random state, random `127.0.0.1` callback, offline access, and combined scopes; both services connected and no token or secret was printed |
| Connected Apps | Microsoft/Discord OAuth | Authenticate and call service APIs | Blocked | External | These future connectors remain unregistered and require their own publisher clients, implementation, consent, scopes, and safe test accounts |
| Repository import | Open existing repository | Inspect branch/sanitized origin and switch to Code/Source Control | Pass | Unit + live E2E | Real Git folder opened in Source Control; an ordinary folder opened in Explorer without false repository state |
| Git clone | Progress/final state | Parse real clone progress and report completion only after exit 0 | Pass | Unit + live E2E | Real local bare clone emitted starting through completed 100%, opened the exact checkout, and matched origin |
| Git clone | Cancellation | Cancel sender-owned in-flight clone without fake success | Pass | Unit + live E2E | Starting progressed to cancelling/cancelled; cancel returned true and no cloned output remained |
| Git clone | URL/error safety | Reject insecure/credential/query URLs and redact stderr | Pass | Unit + live E2E | Invalid HTTP URL reached failed state with a generic safe message and no raw process output |
| Production | Work Mode packaged smoke | Surface, persistence, browser connector, Code preservation, responsive themes | Pass | Packaged E2E + prior live E2E | Refreshed Intel package passed dark and 960×600 light layouts, 12 rapid mode switches, reduced-motion emulation, Escape dismissal, exact tool registration, conversation persistence, real Example Domain browser use, and zero renderer/log exceptions; prior live Gemini streaming smoke passed |
| Design system | Tokens and motion contract | Shared semantic surfaces, spacing, type, shape, elevation, 120–240 ms motion, focus and reduced motion | Pass | Inspection + renderer tests + production build | `OMNICODE_DESIGN_SYSTEM.md` is implemented in the shared shell, mode switch, Work Mode, and Connected Apps foundation |
| Design system | Packaged visual/accessibility sweep | Light, dark, system, reduced motion, resizing and contrast after refreshed styling | Pass | Packaged E2E + visual inspection | Refreshed dark and compact-light screenshots were visually inspected; no horizontal overflow, reduced-motion token `1ms`, modal Escape closure, single-host mode stress, focus semantics, and zero stuck overlays/errors passed |
| Google OAuth | Desktop authorization protocol | System browser, random loopback callback, PKCE S256, state, code exchange, Keychain, refresh, revoke | Pass | Unit + local integration | Real local callback server exercised against mocked Google HTTPS endpoints; tokens never enter argv or renderer state |
| Google OAuth | Developer build-input isolation | Read a local Desktop credentials JSON without copying its path or raw body into the app | Pass | Unit + compiled-output inspection | Strict loader accepts only a bounded absolute Desktop JSON using official endpoints and a loopback redirect; local file is mode `0600` and ignored; path/raw file stay outside tracked source and output while matching extractable Desktop client metadata enters only the trusted main bundle |
| Google OAuth | Packaged publisher configuration | Make the official Desktop public-client metadata available to a Finder-launched app without exposing publisher setup to users | Pass | Unit + production build + packaged UI | Matching client ID/secret metadata and Testing marker enter only the trusted main bundle and are assumed extractable; the renderer receives none, and packaged disconnected UI contains no “OAuth client” or “credentials file” developer language |
| Google OAuth | Authorization failure retention | Preserve timeout, cancellation, invalid-state, test-user, and missing-publisher errors across the shared service refresh | Pass | Unit + real failure reproduction | A real five-minute consent timeout exposed a refresh bug; the manager now retains the sanitized failure for both Gmail and Drive until successful retry or disconnect, with regression assertions |
| Google OAuth | Live account consent and restart | User consent, token exchange, verified identity, and Keychain-backed restart persistence | Pass | Manual/packaged E2E | After test-user approval, the real consent completed, both Gmail and Drive reported the same verified account, a full packaged-app restart remained connected, and renderer storage contained no token fields |
| Google Workspace | Live read-only API access | Call Gmail and Drive through the persisted shared grant without exposing service data | Pass | Packaged E2E | Gmail returned a valid label array and Drive returned a valid empty result for a deliberately impossible query; the audit emitted only counts/booleans and made no writes |
| Gmail | REST tools and confirmation safety | Search/read/thread/attachments/labels/draft/send/reply/read/archive/labels | Pass | Unit + integration | All 13 tools use official endpoints and bearer auth in the main process; MIME construction, HTML fallback, header-injection denial, bounded threads, schemas, abort propagation, network mapping, and zero-call send cancellation are covered |
| Gmail | Attachment transfer and native save | Download exact binary bytes without exposing them to the model; save only to a user-selected path | Pass | Unit + integration | MIME metadata is checked before text injection; opaque transfer IDs preserve exact bytes for Drive/Gmail workflows, and native-save wiring returns only safe filename/size metadata |
| Gmail | Live mailbox operations | Real search/read/draft/send/reply/state/archive/label behavior | Partial | Manual/packaged E2E | Real label listing passed through the packaged app; mutations/send and content-bearing reads were intentionally not performed without disposable mailbox data and exact confirmation |
| Google Drive | REST tools and content safety | Search/list/metadata/read/export/upload/folder/rename/move/copy/trash/restore | Pass | Unit + integration | All 15 tools use official Drive v3 requests; Google Docs/Slides text and Sheets CSV reads, PDF attachment export, multipart text/opaque upload, bounds, invalid-UTF-8 refusal, cancellation, network mapping, and policies are covered |
| Google Drive | Binary transfer and native save | Preserve exact bytes across download, connector upload, and local save boundaries | Pass | Unit + integration | Binary data stays in the trusted transfer store; model-visible results contain opaque IDs and safe metadata rather than base64 or local paths |
| Google Drive | Live Drive operations | Real search/read/export/upload/organize/trash/restore | Partial | Manual/packaged E2E | A real no-result search passed through the packaged app; content reads and writes were intentionally not performed without disposable Drive data and exact confirmation |
| Google Workspace | Cross-connector workflows | Gmail attachment to Drive, Drive file to Gmail draft, Gmail summary, and Drive analysis | Pass | Work-agent integration | Four multi-turn tests exercised the real registry/agent orchestration with controlled Google endpoints; confirmation gates and exact bytes passed, and no binary/base64/path leaked into model messages |
| Google Workspace | Live cross-service account workflow | Repeat transfer/summary workflows against a safe real Gmail and Drive account | Blocked | Packaged E2E | OAuth and minimal read-only service access pass; BLOCKED — DISPOSABLE CROSS-SERVICE TEST DATA AND PER-ACTION CONFIRMATION REQUIRED |
| Work tools | In-flight cancellation | Stop a Work request while a Gmail or Drive operation is executing | Pass | Unit + integration | Abort signal flows from the sender-bound Work operation through Tool Registry into connector fetch; pre-aborted calls make no request and do not report success |
| Work tools | Gmail/Drive result cards | Persist and render useful connector summaries without exposing internal identifiers, transfer IDs, raw bodies, bytes, or paths | Pass | Unit + packaged E2E + visual inspection | Trusted bounded projections render distinct Gmail and Drive cards; both cards survived conversation reload in dark and compact-light 0.3.0 smokes, leaked no internal test identifier, and logged zero renderer errors |
| Work transfers | Opaque binary capability | Private bounded creation, consume, integrity, expiry, and quit cleanup | Pass | Unit + integration | UUID capabilities, `0600` storage, 24 MiB limit, one-hour TTL, hash validation, single trusted directory, and app-quit cleanup pass |
| Work dialogs | Keyboard and rapid-switch stress | Dismiss Connected Apps/Settings with Escape and avoid duplicate hosts/overlays | Pass | Packaged E2E | Dark and compact-light runs each performed 12 rapid switches, opened/closed both dialogs, retained one host per mode, and ended with zero overlays or renderer errors |
| Work approvals | Default and migration | Missing/corrupt settings fail closed to Ask for approval | Pass | Unit | Versioned private store defaults to Ask, rejects invalid modes/connector IDs, bounds size, writes atomically with mode `0600`, and recovers safely |
| Work approvals | Global modes | Persist Ask, Approve for me, and Full access | Pass | Unit + packaged E2E | Quick selector and Settings use one main-process store; Full is refused until its warning is acknowledged |
| Work approvals | Per-connector overrides | Override or inherit the global mode | Pass | Unit + packaged E2E | Gmail override persisted independently and was restored after the audit; null removes an override and returns to global policy |
| Work approvals | Ask policy | Reads automatic; changes and external actions ask | Pass | Unit + packaged E2E | Harmless Gmail labels read made a real API call with no prompt; proposed send opened approval and Cancel made no send call |
| Work approvals | Approve-for-me policy | Safe reversible routine actions run without prompt spam | Pass | Unit + integration | Multi-step Gmail-to-Drive workflow completed low-risk reversible actions while communications, sensitive transfer, destructive, and higher-risk actions retained gates |
| Work approvals | Full policy | Ordinary actions automatic but hard boundaries remain | Pass | Unit | Financial, account-security, critical, irreversible destructive, and explicitly always-confirm actions still require one-time approval |
| Work approvals | Dynamic escalation | Bulk and credential-bearing inputs become critical | Pass | Unit | Bulk-recipient/large-array and credential-field fixtures require direct approval in every mode |
| Work approvals | Connector restrictions | Read-only connector cannot write in Full | Pass | Unit | Static connector access is checked before mode policy and blocks the executor accurately |
| Work approvals | Prompt-injection resistance | Untrusted content cannot lower policy | Pass | Unit + Work-agent integration | Email, Drive, webpage, cloud-provider, and Ollama instructions to skip approval still reached the same main-process gate |
| Work approvals | Provider parity | Cloud and compatible Ollama tool calls share enforcement | Pass | Parameterized integration | OpenAI, Anthropic, Gemini, and Ollama model-produced tool calls all required PermissionManager authorization before future-connector execution |
| Work approvals | Approval card | Show app, risk, reason, exact safe parameters, Cancel and Approve once | Pass | Renderer unit + packaged E2E + visual inspection | Gmail card displayed exact fake recipient, subject, message, and action; secret fields/internal IDs were excluded and safe Cancel was focused |
| Work approvals | Cancellation/lifecycle | Cancel, timeout, stop, navigation, and concurrent requests fail safely | Pass | Unit + packaged E2E + inspection | User Cancel returned before executor; sender-bound cleanup cancels pending requests and a concurrent request uses native macOS fallback instead of replacing the visible card |
| Work activity | Private safe history | Persist only bounded redacted action metadata | Pass | Unit + packaged E2E | History is atomic/private, capped at 500, excludes inputs/results/content/tokens, renders success/failure/cancel/blocked states, and can be cleared |
| Work approvals | Full warning UI | Explain risk before enabling and preserve mode on Cancel | Pass | Renderer + packaged E2E | Warning defaults to Keep current setting; packaged Cancel retained Approve for me and did not acknowledge Full |
| Work approvals | Live Gmail no-write safety | Exercise real account without sending mail | Pass | Packaged live E2E | A fake invalid-recipient send was proposed and cancelled; no Gmail write occurred, Activity recorded user-cancelled, original permission settings were restored, and renderer errors were zero |
| Code Agent | Shared agent architecture | Reuse provider tool turns, Tool Registry, and Permission Manager without a renderer bypass or second protocol | Pass | Unit + inspection | `CodeAgentManager` supplies only application-owned Code descriptors to the existing `WorkAgentManager`; every executor call receives the backend authorization decision and task identity |
| Code Agent | Provider tool loop | Inspect, call a real tool, observe its result, and complete only after provider response | Pass | Unit + packaged live Gemini | Controlled loop returned real executor content; packaged Gemini inspected a disposable workspace, created exact bytes, read the file, and completed truthfully |
| Code Agent | Workspace file changes | Create/update/delete through Diff Manager and retain review/undo | Pass | Integration + packaged live Gemini | Real temporary files matched accepted changes, proposal ID and line counts were returned, undo restored disk; packaged model write exposed a real proposal |
| Code Agent | Real terminal ownership | Start/wait/observe/input/interrupt/stop bounded task-owned PTYs and return actual exit state | Pass | Unit wiring + existing packaged PTY E2E | Agent sessions use the same real `node-pty` and login-shell environment already proven in the package; ownership, output, exit, Ctrl+C, cleanup, and secret-prompt blocks have focused regression coverage |
| Code Agent | Build/test commands | Stream bounded redacted progress and wait for the actual process exit | Pass | Unit + integration | Build/test descriptors route to the task-owned PTY; live-progress forwarding and exit-result plumbing pass without detached fake-success states |
| Code Agent | Dependency installation policy | Allow only structured project dependency commands and require direct approval | Pass | Unit | npm/pnpm/yarn/bun/Cargo/Go project forms are validated; shell metacharacters, global/system installation, Homebrew, sudo, and download-execute forms are blocked |
| Code Agent | External folder grant | Use a native exact-folder selection and task-bound opaque grant | Pass | Integration | Real temporary external roots were selected by the controlled picker; cross-task access, sensitive paths, traversal, and system roots are rejected; grant always requires confirmation |
| Code Agent | External file creation | Atomically create exact bytes within the selected external root | Pass | Integration | Real nested temporary file matched exact expected bytes; one grant confirmation occurred and `../` escape was rejected |
| Code Agent | Workspace transition | Open a directory inside a selected external grant only after direct approval | Pass | Unit + inspection | Canonical directory/type/boundary checks and `confirmation: always` precede the existing trusted workspace activation path |
| Code Browser | Public web search | Generate a credential-free HTTPS search URL and return bounded visible results | Pass | Unit + shared packaged-browser E2E | Exact encoded Google query, DNS guard, isolated partition, visibility policy, and bounded page snapshot pass; the underlying public Managed Browser previously loaded a real HTTPS site |
| Code Browser | Localhost testing | Open only loopback HTTP/HTTPS, read page, reload, inspect console, and preserve Work Browser rules | Pass | Unit + shared packaged server/browser E2E | Code accepts localhost and rejects other private addresses; Work remains HTTPS/public-only; existing packaged server/browser audit proves the reused real page and local-server layers |
| Code Browser | Safe interaction | Click non-form controls and type non-secret text without submitting | Pass | Unit + inspection | Credential fields, forms, login/account/security, destructive, and financial controls remain blocked; permissions, downloads, and popups are denied |
| Code Agent | Allowlisted app launch | Launch a fixed macOS development app with an optional workspace target and focus policy | Pass | Unit + macOS integration | Exact argument-array construction, app allowlist, path boundary, and `-g` Never-focus behavior pass; Finder opened a safe temporary folder and TextEdit application resolution succeeded through real `/usr/bin/open` |
| Code Agent | macOS permission state | Report Accessibility/Screen Recording and open the exact privacy pane | Pass | Unit + inspection | Permission reads fail honestly to unavailable; System Settings URLs are fixed application values, not model-controlled strings |
| Computer control | Arbitrary external-app observe/click/type | Use a structured native controller rather than shell automation | Partial | Unit/integration + live read-only host test | Omni has a fixed Swift CGEvent helper and task-owned tools; live pointer/frontmost-app observation passed. Semantic AX/screen targeting, live input E2E, and Code-Agent exposure remain unavailable; arbitrary AppleScript/screenshots stay blocked |
| Code Agent | Approval independence | Keep Ask/Approve-for-me/Full separate from Standard/Glasses visibility | Pass | Unit + renderer inspection | Approval flows only to trusted authorization; visibility changes event filtering/persistence only and cannot grant authority |
| Code Agent | Ask mode | Deny a proposed write before executor/disk mutation | Pass | Integration | Rejected confirmation produced a cancellation error and the real source file remained byte-identical |
| Code Agent | Full Access hard boundaries | Allow ordinary registered work while preserving always-confirm and command/path blocks | Pass | Unit + integration | External grant and dependency install still require confirmation; credentials, protected roots, privilege escalation, and disallowed commands remain blocked |
| Code Agent | Pause/resume | Pause at the next atomic-action boundary and resume the same task | Pass | Unit | State moves running→pausing→paused→running without interrupting a half-applied file operation |
| Code Agent | Stop | Abort provider/tool work, stop owned processes/server, revoke grants, and retain completed edits | Pass | Unit + inspection | Stop reaches a terminal state, cleanup is task-scoped, and no automatic rollback is claimed |
| Glasses Mode | Live timeline | Show all observable action events while Standard shows concise consequential updates | Pass | Unit + packaged live E2E | The successful Gemini task rendered five timestamped events, completion summary, and Review Diff action after restart-safe preference loading; Standard/Glasses filtering passes regression coverage and the package logged zero renderer errors |
| Glasses Mode | Safe persistent activity | Restart, update, bound, clear, and recover the private task/event store | Pass | Unit | Atomic `0600` store survives a new manager instance, updates event IDs, preserves active tasks on clear, bounds task/event/output volume, and recovers from corrupt JSON |
| Glasses Mode | Secret redaction | Prevent keys, passwords, tokens, headers, and control characters from reaching history/UI | Pass | Unit | Commands, summaries, outputs, result text, and on-disk JSON redact representative secret assignments and bearer data |
| Code Agent | Prompt-injection boundary | Treat file/browser/terminal/Git/app content as untrusted data | Pass | Unit + inspection | The immutable Code system contract explicitly says untrusted tool data is never instruction authority; the model has no route around registered backend policy |
| Code Agent | Current Google model default | Avoid retired Gemini 2.5 fallback and prefer account-visible current models | Pass | Unit + real provider failure | A real 2.5 request returned Google's retired-model 404; catalog ordering/fallback now prefers latest and 3.6, and the successful packaged Code task used an available current model |
| Code Agent | Provider quota failure | Surface real Gemini quota exhaustion without disk fake-success | Pass | Packaged live E2E | Google returned a real 429 after the successful task; Code Agent ended failed with the provider error and made no success claim |
| Regression | 0.5.0 Code Agent baseline | Full automated suite and production TypeScript/bundle health after the original Code Agent milestone | Pass | Automated | 454 tests passed, one native Keychain test was intentionally skipped, typecheck passed, and the 0.5.0 production bundle compiled |
| Code Agent plan | Provider-neutral contract | Supply one bounded plan schema to OpenAI, Anthropic, Gemini, and compatible Ollama paths without private-reasoning APIs | Pass | Unit + inspection | Application-owned `agent.update-plan` is a Code-only metadata tool; shared provider adapters translate the same descriptor and no provider-private reasoning is requested |
| Code Agent plan | Initial task understanding | Persist and display task interpretation, Reasoning Summary, ordered steps, current step, and next step | Pass | Unit + packaged E2E | A safe immediate system plan appears while the provider responds; the controlled model then replaced it with a validated model-authored plan |
| Code Agent plan | Dynamic progress and decisions | Update completed count, current/next step, decisions, assumptions, and explained course changes | Pass | Unit + packaged E2E | Packaged five-turn audit reached 2/2 complete, retained the explained user-driven change and assumption, and rendered Decision events |
| Code Agent plan | Modify Plan | Pause safely, accept a bounded user course correction, deliver it before more provider work, and resume | Pass | Unit + packaged E2E | UI submitted “Do not install dependencies”; task moved pausing→paused→running, provider received the correction, and the revised scope appeared in the plan |
| Code Agent plan | Skip Step | Mark the active step skipped and require replanning before another action | Pass | Unit + packaged E2E | UI skip created a persisted plan event and queued a task-owned intervention; the model produced a changed plan after Resume |
| Code Agent plan | Stale-call safety | Do not execute calls proposed before a user intervention | Pass | Unit + packaged E2E | Controlled model's delayed stale call was returned as skipped, never reached the executor, and the next provider turn replanned from the user course |
| Agent visibility | Standard planning | Keep plan/reasoning/progress/final report plus consequential activity while hiding low-level actions | Pass | Unit + packaged E2E | Standard retained the complete plan and report and showed 7 consequential events versus 9 in Glasses |
| Agent visibility | Glasses plan/action timeline | Combine plan, decisions, reasons, real actions, and results without layout overflow or raw private reasoning | Pass | Unit + packaged E2E + visual inspection | Real runtime detection appeared only in the expanded Glasses action timeline; action reason, changed/progress plans, no horizontal overflow, and zero renderer errors passed |
| Code Agent plan | Persistence, redaction, and migration | Survive reopen, redact credential-like plan/event text, and load legacy tasks without plan state | Pass | Unit | Private atomic store persisted plan revisions and action reasons, removed representative bearer/key values, and retained backwards compatibility |
| Code Agent | Final task report | Derive What I did/changed/tests/results/problems/plan changes/remaining issues from observed activity | Pass | Unit + packaged E2E | Completed package rendered every required section; absent file/test evidence was stated as unrecorded instead of implied success |
| Regression | Existing Code and Work systems after Agent plan addition | Full automated suite, TypeScript, production bundles, package architecture, and native development module | Pass | Automated + packaged | 459 tests pass, one native Keychain test is intentionally skipped, typecheck/build pass, 0.5.1 x64/arm64 packages verify, and development `node-pty` was restored to x86_64 |
| Omni baseline | Clean expansion checkpoint | Preserve a recoverable verified baseline before Omni source changes | Pass | Git inspection | `399e4f0` was clean before Omni design/Phase 1 work began; the verified floor is 459 passing tests, one intentionally skipped native Keychain test, passing typecheck/build, and verified 0.5.1 packages |
| Omni design | Architecture/security/background/voice/cursor records | Keep implementation status and remaining native/release gates distinct | Pass | Repository inspection | Five records now document the implemented controller/tool/store/UI/overlay/TTS foundation and explicitly unavailable helper/STT/wake/cursor capabilities |
| Omni Phase 1 | Shared contracts | Validate bounded Omni modes, task/settings/state schemas, normalization, and corrupt/oversize rejection | Pass | Unit + TypeScript | Contract consumers and normalization paths pass in the complete serial suite and typecheck |
| Omni Phase 1 | Settings persistence | Verify private atomic global settings, fail-closed defaults, updates, corruption recovery, Full Access acknowledgement, retention, and no credential storage | Pass | Unit | Mode `0600`, atomic writes, validation, recovery, concurrency, bounded values, and warning enforcement pass |
| Omni Phase 1 | Task/activity store | Verify private bounded persistence, redaction, recovery, clear, retention, and interrupted-task restart handling | Pass | Unit | Raw prompts and generic raw tool results are not persisted; active clear protection, stale-task stop recovery, secret/path/URL redaction, limits, and pruning pass |
| Omni Phase 1 | Main-owned controller | Start, plan, tool loop, pause/resume/stop, mode/plan changes, history, errors, and shutdown | Pass | Unit/integration | Concurrent start is serialized; pending approvals and old calls are invalidated by action generation; missing model fails clearly and built-app smoke confirmed no false history entry |
| Omni Phase 1 | Third-mode shell | Switch Code → Work → Omni, preserve hosts/state, persist validated mode, and render honest capability controls | Pass | Renderer unit + built-app live smoke | Mode/preference tests pass; built output visibly rendered Code/Work/Omni and the full Omni settings/task surface. Packaged installer state-preservation/compact/theme audit remains |
| Omni regression | Existing Code/Work regression gate | Run the complete established suite, typecheck, and production/native-helper build after Omni wiring | Pass | Automated + packaged smoke | 66 files/561 tests pass with one intentionally skipped native-Keychain file/test; typecheck, production bundles, x86_64 helper, and x64 directory package pass; packaged core/Omni smoke reports zero renderer errors |
| Omni tools | Unified mode-aware authorization | Route through source Tool Registry and one PermissionManager; compose the stricter Work/Omni policy | Pass | Unit/integration | Code/Work descriptor collisions remain source-scoped; disconnected/unscoped connectors stay absent; Full cannot lower connector or hard-boundary policy |
| Omni permissions | Active-task policy display | Show and lock the immutable approval policy captured at task start | Pass | Renderer unit + inspection | Running task displays `task.approvalMode` and disables the selector; future global-default edits cannot imply that active authority changed, and main-process enforcement remains authoritative |
| Agent terminal security | Direct approval and clean process boundary | Require confirmation in every approval mode; skip shell rc/login files; strip credentials/auth sockets from environment | Pass | Unit + inspection | All run/start/input paths are always-confirm; zsh/bash/fish use no-rc arguments; only allowlisted toolchain/locale environment survives. Sixteen focused security tests and typecheck pass after the hardening |
| Omni tools | Isolated Invisible browser and focus policy | Keep Omni browser session separate, suppress page/app foregrounding in Invisible mode, preserve validation | Pass | Unit/integration | Dedicated `persist:omnicode-omni-browser-v1` partition and mode-aware visibility tests pass; packaged live browser workflow remains |
| Omni tools | Public/localhost session isolation | Prevent public browsing state from crossing into localhost and revalidate public DNS | Pass | Unit/integration | Public and loopback pages use separate partitions; scope switches destroy/clear the old page, loopback binds to one exact origin, and public DNS is resolved on each request |
| Omni tools | Model-result secret boundary | Recursively remove credentials/auth headers/cookies/private keys and bound tool data before the next provider turn | Pass | Unit + Work-agent integration | Sensitive key containers, token patterns, URL secret parameters, depth/nodes/bytes, and actual tool-loop model visibility are covered |
| Omni controller | Failed tool cannot end green | Derive final task outcome from observed tool failures even if the model returns completion text | Pass | Unit | A final failed or cancelled latest tool state forces the Omni task to Failed rather than accepting a false success summary |
| Omni background | Global shortcut while app is resident | Register/unregister `⌘⇧Space` and activate overlay from another foreground app | Pass | Built-app live smoke + unit | Real shortcut reopened the overlay while another app was foreground; shortcut replacement/conflict and packaged Space/closed-window focus matrix remain |
| Omni background | Restricted overlay capability boundary | Build dedicated surface/preload and prevent access to full renderer APIs | Pass | Built-app live smoke + inspection + production build | Overlay had `window.omnicode === undefined`; only `window.omniOverlay` settings/tasks/activation groups existed; exact window/frame/URL/channel checks guard main handlers |
| Omni background | Packaged login background launch | Initialize Omni without main window/Dock on login or explicit background argument | Pass | Unit + inspection | Packaged-only launch detection uses macOS login state or `--omni-background`, skips main-window creation, hides Dock, and keeps development launches visible; packaged login E2E remains |
| Omni background | Post-quit native helper | Verify helper ownership, update, authenticated IPC, duplicate suppression, and both architectures | Blocked | External + not implemented | No native helper project exists; explicit post-quit shortcut activation additionally requires signing/notarization and matching hardware tests |
| Omni voice | macOS speech output | Verify availability, installed voices, safe invocation/text/rate, completion, Stop, abort, supersession, and disposal | Pass | Unit + host integration + built-app smoke | Fixed `/usr/bin/say`, no shell, stdin text, validation/redaction/output bounds pass; production route returned real availability and installed voice list |
| Omni voice | Push-to-talk STT | Verify real mic capture, partial/final transcript, interruption, permissions, and no raw-audio persistence | Not Run | Not implemented | Provider contract exists, but no native microphone/STT provider or required packaging capability exists |
| Omni voice | Local “Hey Omni” activation | Verify offline pre-wake processing, zero pre-wake network traffic, false-trigger/resource soak, pause/disable, and lock/logout cleanup | Blocked | External + not implemented | No licensed bundled wake model/engine or signed capturing helper has been selected and verified |
| Omni Cursor | Structured native command boundary | Verify observe/move/click/double-click/scroll/type/key/focus through a fixed native service | Partial | Unit/integration + host read-only smoke | Strict request/response/version/ID/size/time/abort validation, command/key/app allowlists, direct input approval, secret-text denial, x64/arm64 compile, and live `observe` pass; live input and semantic AX/screen tests remain |
| Omni Cursor | Human takeover and emergency stop | Invalidate the execution generation below the renderer and prevent all queued later actions | Pass | Unit/integration + packaged live E2E | Pointer polling and smooth-move takeover pause the task. The main-process `⌘⇧Esc` shortcut is required for readiness; posting the real chord through the packaged helper stopped a live Google-backed Cursor task, aborted/cleaned it, and produced the exact stopped result. A helper-owned event tap remains defense-in-depth work |
| Omni Cursor | UI readiness and mode routing | Enable only when helper and Accessibility are ready; expose native tools only in Cursor mode | Pass | Unit + typecheck + live helper smoke | Main API reports exact status, UI disables unavailable Cursor mode, Invisible catalog excludes computer tools, and `using-cursor` appears only during real computer actions |
| Omni security | Secret/history boundary | Keep credentials, raw input, raw generic tool results, and full external content out of history/overlay/model tool results | Pass | Unit + built-app boundary smoke | Store/router/controller redaction and recursive model-result sanitizer tests pass; overlay exposes only sanitized task/settings state and no full preload |
