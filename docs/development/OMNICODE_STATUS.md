# OmniCode Stabilization Status

Last updated: 2026-09-23

## OmniCode 0.7.0 permission/overlay stabilization

- ✅ The global shortcut now opens one compact 320×248 voice-only overlay in
  the active display's top-right work area without forcing focus.
- ✅ The overlay contains no typed input, plan, Activity, history, model, or
  execution-policy UI. It shows bounded transcript, actual microphone amplitude,
  high-level task state, speech state, contextual permission recovery, and a
  separate Stop Task action.
- ✅ `MacOSPermissionManager` now owns operating-system detection, request,
  explanation metadata, exact Settings routing, return refresh, and broadcast.
- ✅ Setup and Settings invoke the real manager. A denied optional permission
  does not block Invisible Mode, and scoped Automation/file access is no longer
  presented as a global success toggle.
- ✅ Speech authorization can be requested before recognizer availability is
  checked, so a missing on-device asset no longer prevents the macOS permission
  workflow itself.
- 🟡 Current host limitation: `en-US` on-device Speech recognition remains
  unavailable, so live transcription cannot pass on this Mac even after the
  authorization request path is fixed.
- 🔵 Public identity remains blocked by Developer ID signing/notarization; the
  development and internal installer helpers are ad-hoc signed.

Status legend:

- ✅ Working & Verified
- 🟡 Partially Working
- 🔴 Broken
- ⚪ Not Implemented
- 🔵 Blocked / External Requirement

This file records observed repository behavior, not specification intent. A UI
surface alone is not evidence that its backing operation works.

## Baseline

- Architecture: Electron 44 main process + sandboxed React 19 renderer + typed
  CommonJS preload bridge; TypeScript throughout; no Tauri or Rust.
- Build: electron-vite and electron-builder, macOS 14 minimum, separate x64 and
  arm64 DMG/ZIP artifacts, native `node-pty` unpacked from ASAR.
- Git: `master` had no commits at audit start. Stabilization checkpoint
  `28bb077` preserves the verified 0.1.1 state; Work Mode continues on
  `codex/work-mode`. No user changes were discarded.
- Omni expansion baseline: commit `399e4f0` (`Add visible Code Agent planning`)
  was a clean, recoverable 0.5.1 checkpoint before Omni design and Phase 1
  work began. The current Omni files are active in-progress work and are not
  part of that verified baseline; no existing user changes were discarded.
- Baseline launch: packaged Intel app started on macOS Sonoma without a white
  screen or captured renderer exception. Setup, workspace IPC, real zsh PTY,
  localhost server, and remote-navigation blocking passed the existing smoke.
- Baseline tests: 185 automated tests passed; the native Keychain test was run
  separately and passed. This is evidence for covered paths only.
- Current verification: OmniCode 0.5.1 has 459 automated tests
  passing with one native Keychain test intentionally skipped from the normal
  run. TypeScript and production compilation pass. The packaged 0.5.0 Intel app
  passed startup/workspace, real zsh PTY, localhost/public-secret boundary,
  navigation, live Gemini Code Agent, persistent Glasses timeline, diff-action,
  and zero-renderer-error checks. The packaged 0.5.1 Intel app additionally
  passed a controlled local five-turn Agent planning audit covering the visible
  course of action, concise reasoning summaries, current/next progress,
  decisions, assumptions, user plan editing, skipped steps, stale-call
  cancellation, safe pause/resume, Standard/Glasses filtering, final reporting,
  layout bounds, and zero renderer errors. The packaged
  0.4.0 Intel app passed the new Work action policy audit: global and connector
  modes persisted, Full Access required a warning, a safe Gmail read ran without
  prompting, and cancelling an exact Gmail-send card prevented the network call
  and produced safe local activity with zero renderer errors. The packaged
  0.3.1 Intel app previously passed real shared-Google-account
  authorization, verified identity, Keychain-backed restart persistence, and
  read-only Gmail/Drive API probes without exposing account data. The earlier
  0.3.0 Intel app passed dark and compact-light
  Work Mode smokes covering mode separation, persistence, Managed Browser,
  all 13 Gmail and 15 Drive tool registrations, reduced motion, Escape dismissal,
  rapid mode switching, bounded Gmail/Drive result cards, horizontal-overflow
  bounds, and zero renderer errors.
  A three-launch packaged soak also passed fresh onboarding, workspace startup,
  idle-resource, quit, and child-process cleanup bounds. The versioned 0.5.1
  Intel and Apple Silicon DMG/ZIP artifacts pass archive/image integrity,
  executable/native-module architecture, embedded-version, original-icon, and
  checksum verification. The Intel package ran the smokes; arm64 execution still
  requires matching Apple Silicon hardware.

- Current Omni verification: the complete serial run passed 70 test files with
  601 tests passed and one intentionally skipped native-Keychain file/test.
  TypeScript checking and the complete multi-entry production build passed,
  including the dedicated `omni-overlay.cjs` preload and
  `omni-overlay.html` renderer. A built-app live smoke rendered the
  Code/Work/Omni selector, enabled Omni, opened the restricted overlay, verified
  that `window.omnicode` is absent there while only `window.omniOverlay`
  settings/tasks/activation groups exist, reopened it with real `⌘⇧Space`
  while another app was foreground, queried real TTS availability and the
  installed macOS voice list, and verified a missing selected model fails
  clearly without creating task history. Approval-display, agent-terminal,
  model-result, browser-isolation, and native-Cursor hardening pass the complete
  serial suite, typecheck, 3,122-module production build, x86_64 native-helper
  build, x64 package, and packaged core/Omni readiness smoke. The 0.7.0 x64
  installer-level overlay audit also verified restricted IPC, permission state,
  no focus theft, compact geometry, and zero renderer errors.

- Current voice-first UI verification: the nine-step first-run setup, compact
  dashboard, opt-in text fallback, Settings → Omni sections, legacy-settings
  migration, and production audit are implemented. The fresh-profile audit
  advanced all nine steps, verified the Core/microphone/four command selectors/
  Activity structure, confirmed the removed permanent Course/History/
  Availability panels stay absent, checked 960×600 and 720×720 layouts,
  validated Reduce Motion and persistence, and recorded zero renderer errors.
  TypeScript and the complete 3,122-module production build pass.

- Current distribution: OmniCode 0.7.0 x64 and arm64 DMG/ZIP artifacts were
  rebuilt after the compact-overlay and permission work. Both disk images pass `hdiutil verify`
  and mounted-content/version/architecture checks; both ZIPs pass integrity
  testing. The x64 packaged app passed the release smoke with a real PTY,
  localhost security boundary, Omni native readiness, navigation denial, and
  zero renderer errors. Arm64 execution still requires Apple Silicon hardware.
  Both builds are unsigned and unnotarized because no Developer ID certificate
  is installed.

## Omni implementation status

The five Omni records (`OMNI_ARCHITECTURE.md`,
`OMNI_BACKGROUND_SERVICE.md`, `OMNI_CURSOR_MODE.md`, `OMNI_SECURITY.md`, and
`OMNI_VOICE.md`) now record both the implemented foundation and the remaining
native/release gates.

- Phase 1 contracts, settings, private task history, provider-neutral
  controller, mode-aware router, full Omni UI, shared authorization, connected
  app filtering, isolated browser, and typed Invisible-mode task flow are
  implemented and focused-test verified.
- The third Code/Work/Omni selector and preference validation are implemented;
  all three hosts remain mounted. A packaged three-mode state-preservation audit
  remains to be recorded.
- Resident-process `⌘⇧Space` registration, an optional menu-bar item, a
  dedicated capability-limited overlay, and packaged main-app login registration
  are implemented. The built-app live smoke verified global activation while
  another application was foreground. Packaged login startup can initialize
  Omni without a main window or Dock icon. Another-Space/closed-main-window
  packaged testing remains; there is no lightweight post-quit helper.
- Real macOS speech output is implemented with fixed `/usr/bin/say`. A separate
  fixed-protocol Swift helper now implements explicit push-to-talk with
  `AVAudioEngine` and `SFSpeechRecognizer`, required on-device recognition,
  bounded partial/final transcripts, Stop/Cancel, typed IPC, honest permission
  state, no renderer media access, and no raw-audio persistence. The current
  Mac lacks the `en-US` on-device Speech asset, so capture is correctly disabled
  pending that external asset; local “Hey Omni” wake remains unavailable.
- A fixed-protocol Swift CGEvent helper, task-owned TypeScript service, eight
  structured computer tools, Accessibility readiness gate, direct approval for
  input, secret-text rejection, and polling/movement takeover pause are present.
  A packaged disposable-TextEdit audit now verifies pointer movement/restoration,
  click/double-click, scroll, exact Unicode typing/save, key input, and app
  focus. General semantic AX/screen targeting, a helper-owned stop event tap,
  signed packaged identity, and broader adversarial input remain incomplete.
- The remaining helper/input/cursor phases carry external release risks: a stable
  signed helper identity requires Apple Developer ID signing, notarization, and
  architecture-specific execution testing; local wake support also requires a
  licensed, bundled, measured wake engine/model. These are not represented as
  working while unresolved.

## Subsystem inventory

| Subsystem | Status | What exists / UI / backend connection | Tests performed and result | Bugs or fixes | Remaining work |
| --- | --- | --- | --- | --- | --- |
| Application startup | ✅ Working & Verified | Electron lifecycle, single-instance handling, secure window options, first-launch setup, React workbench, macOS menu | Fresh packaged launch and console capture passed; refreshed three-cycle soak verified onboarding and reached shell/workspace in 5.4/7.1 s cold and at most 4.3/4.7 s warm, used 392–441 MiB RSS and 1.4–2.6% settled CPU, then quit in 0.6–1.1 s with complete helper cleanup; resize/minimize/zoom/full-screen/close/reopen passed | Removed terminal idle repaint load; added a bounded preload queue; repaired the reusable soak so a genuine fresh-install onboarding surface is validated instead of misreported as a timeout | None in the tested Intel Sonoma lifecycle |
| Code / Work mode boundary | ✅ Working & Verified | Shared persistent `Code` / `Work` switcher; Code workbench remains mounted while Work has an independent conversation surface | Renderer tests and refreshed packaged smokes performed 12 rapid switches in dark and compact-light layouts, retained exactly one Code and Work host, and returned with Work visible and no stuck overlays | Added a validated persistent mode preference, Code-only command/drop routing, and a reduced-motion-aware sliding selection indicator without remounting either mode | None in the tested packaged mode lifecycle |
| Omni design and safety records | ✅ Working & Verified | Five permanent records distinguish the implemented controller/tool/persistence/overlay/TTS foundation from remaining helper/STT/wake/cursor targets | Repository and implementation inspection completed; records preserve explicit test/release gates | Replaced stale design-only claims with evidence-backed current disposition | Keep synchronized with implementation and remove no gate without evidence |
| Omni voice-first setup/dashboard/settings UI | ✅ Working & Verified | Bounded contracts; private atomic settings/task stores; nine-step setup; central state-reactive Core; provider/model/execution/approval command bar; current task; redacted Activity; push-to-talk dock; secondary history; opt-in typed fallback; Settings → Omni | Focused settings/renderer tests, typecheck, complete production build, and fresh-profile production CDP audit pass. The audit exercised all nine steps, 960×600 and 720×720 layouts, Reduce Motion, input preference/persistence, and zero renderer errors | Replaced the dense card dashboard, removed fake-permanent Course/Availability emphasis, made typed input default-off, added real setup/persistence and friendly error mapping, fixed a bottom-row dead-space layout bug, kept the optional composer inside its dock/viewport, and disabled submission when Omni is off | Live microphone transcript remains host-blocked by the missing on-device Speech asset; configured-model tool success remains externally blocked by provider availability; public signed installer audit remains |
| Omni tool routing and Invisible mode | ✅ Working & Verified | One shared PermissionManager; source-aware Code/Work routing; stricter connector policy composition; connected/scope-filtered Gmail/Drive catalog; dedicated isolated Omni browser | Router/controller/permission/browser tests and complete serial regression pass; build passes. New repeatable packaged audit requires exactly one real `runtime.detect` call and rejects extra actions; two live Google attempts returned transient HTTP 503 before any tool call, and Omni ended Failed without false success | Removed policy-lowering path, disconnected connector exposure, shared-browser session/focus collision, visible app launch in Invisible mode, and ambiguous live-audit acceptance criteria | Successful packaged configured-model workflow remains **BLOCKED — EXTERNAL PROVIDER AVAILABILITY REQUIRED**; real connector/browser/code-tool matrix remains |
| Agent terminal security | ✅ Working & Verified | Every model-driven terminal run/start/input requires direct approval; agent zsh/bash/fish skip login/rc files and inherit a credential-stripped allowlisted toolchain/locale environment | Focused security tests, complete 601-test serial run, typecheck, and production build pass | Removed Full Access bypass risk, shell-profile credential loading, arbitrary inherited exports, and active-task authority-display mismatch | Packaged approval/environment audit remains |
| Omni global activation/background overlay | 🟡 Partially Working | Configurable resident global shortcut, optional tray, restricted overlay renderer/preload, main-owned state, packaged login background launch without main window/Dock | Built-app live smoke verified `⌘⇧Space` from another foreground app and exact restricted overlay globals; background-launch selection/login settings are unit-tested; production entries build | Added exact overlay window/frame/URL/channel checks, pending main activation, safe native approval owner, and no-window packaged login startup | No post-quit native helper; packaged login/Space/closed-window/focus/conflict audit remains |
| Omni speech output / voice input / local wake | 🟡 Partially Working | Real macOS `say` TTS plus a signed Swift Apple Speech helper, provider-neutral STT service, bounded transcript/amplitude IPC, explicit authorization, and push-to-talk UI; local wake remains absent | Focused voice/UI tests and the 601-test serial suite pass; the x64 package contains both usage strings and an entitlement-signed helper; its packaged status/UI smoke passed with zero renderer errors and accurately reported not-determined permissions plus an unavailable `en-US` on-device asset; both helpers also build/sign as arm64 | Added fixed executable/no shell, transcript/protocol/output/time bounds, on-device-only enforcement, permission request independent from recognizer availability, Stop/Cancel/parent-EOF teardown, usage descriptions, audio entitlement, and hidden-mode cancellation | Install/enable a macOS on-device Speech asset, then run real microphone grant/deny/partial/final packaged tests; stable Developer ID signing/notarization, audible packaged TTS, arm64 hardware execution, and local wake remain gates |
| Omni Cursor Mode | 🟡 Partially Working | Fixed-protocol Swift helper plus task-owned service expose observe/move/click/double-click/scroll/type/key/fixed-app focus only in Cursor mode; observations include non-sensitive frontmost-window geometry; focused secure/password roles and input to OmniCode/unallowlisted apps are rejected; UI gates on helper, Accessibility, and main-process emergency stop | Adapter/session/tool/controller tests pass; x86_64 and arm64 helpers compile; packaged disposable TextEdit/Safari audit passed move/restore, click/double-click, scroll, focus, exact Unicode type/save, key input, unallowlisted-app denial, and secure-field denial; x64 package reports all readiness gates; a real Google-backed Cursor task was stopped with actual `⌘⇧Esc` | Fixed long-move false takeover, asynchronous pointer completion, dropped Unicode CGEvents, added selected-text AX input without clipboard use, native and service target allowlists, focused secure-field refusal, strict protocol/limits, direct approval, secret blocking, takeover pause, renderer-independent stop, and cleanup | Semantic AX/screen targeting, system-authorization/dialog adversarial matrix, helper-owned stop defense, signed/notarized identity, and Apple Silicon runtime test remain |
| Shared UI design system | ✅ Working & Verified | Semantic light/dark surface, type, spacing, radius, elevation, motion, focus, and reduced-motion tokens documented and used by the shared shell plus refreshed Work/Connected Apps surfaces | TypeScript, production bundle, 11 focused renderer tests, dark and 960×600 light packaged screenshots, horizontal-overflow checks, `prefers-reduced-motion` emulation, keyboard dismissal, and rapid-switch stress pass | Replaced ad hoc shell/mode/Work values with shared tokens; raised tiny interactive labels; added restrained 120–240 ms motion, 1 ms reduced-motion overrides, accessible live toast states, and Escape dismissal for Connected Apps and Settings | Continue ordinary visual regression checks as UI code changes; no known defect remains in the tested surfaces |
| Work conversations | ✅ Working & Verified | Main-process private atomic JSON store; create/open/search/rename/pin/delete, messages, Copy/Edit/Regenerate, bounded recovery | Unit tests cover validation, ordering, concurrent writes, private modes, corruption/recovery, and bounds; packaged smoke created, updated, searched, reopened, and deleted a real conversation | Conversation state moved out of renderer-only storage; retries replace the same exchange instead of duplicating it | None within documented limits |
| Work AI transport | ✅ Working & Verified | Provider-neutral Work agent, native provider streaming, cancellation, bounded tool loop, and persisted partial/final messages | Stream/parser/tool-turn tests pass; live Gemini 3.7 streamed exact responses, Stop retained a cancelled partial response, and a prior packaged live run returned the exact expected streamed marker | Fixed Google tool schemas and thought signatures, Ollama tool wire format, response-size/time bounds, and stop-generation cleanup | OpenAI/Claude success and Ollama inference remain externally blocked by missing configured services |
| Model catalog / selectors | ✅ Working & Verified | Authenticated OpenAI/Anthropic/Gemini discovery with bounded cache/fallback plus inspected Ollama metadata; selectors in Work, Code Chat, and Settings | Discovery pagination, filtering, caching, redaction, stale/error states and UI selectors pass tests; live Google returned 20 account-visible models and Code/Work requests respected Gemini 3.7 | Removed manual cloud model-ID fields; preserved explicit unknown capability states | Other providers need live credentials; account/model availability can change |
| Connected apps / tools | 🟡 Partially Working | Main-process Tool Registry, Connector Manager, Permission Manager, Managed Browser, Gmail, and Google Drive adapters; Settings/Work display one shared Google account plus real service permissions and actions | Schema/mode/scope/permission/timeout/result/cancellation bounds pass; packaged Managed Browser passed; x64 package exposed exactly 3 Browser, 13 Gmail, and 15 Drive tools; a real shared Google account authorized both services and completed safe read-only API probes; focused OAuth/API and four controlled cross-workflow tests pass | Added real Google system-browser OAuth, Keychain token records, refresh/revoke, bounded opaque transfers, native save boundaries, exact email confirmation, binary-context refusal, shared user-facing account UI, and abort propagation from Work request through tool fetch | Live Gmail/Drive mutations and cross-service transfers still require explicit confirmation plus disposable test data; Microsoft/Discord remain external requirements |
| Work action approvals | ✅ Working & Verified | Global Ask/Approve-for-me/Full modes, per-connector overrides, fixed tool category/risk/reversibility metadata, trusted main-process enforcement, polished approval card/native fallback, and private local activity | 419-test regression covers all modes, hard boundaries, escalation, cancellation, prompt injection, cloud/Ollama parity, and workflows; packaged 0.4.0 UI verified warning/cancel, persistence, automatic Gmail read, exact send card, no-call cancel, activity, and zero renderer errors | Replaced static action-only prompting with a versioned fail-closed policy; Full cannot bypass critical/financial/account-security/irreversible/always-confirm actions; concurrent renderer requests fall back to a native sheet | None in the tested policy boundary; live user-approved Gmail/Drive writes still require safe disposable data |
| Google Workspace OAuth | ✅ Working & Verified | Shared main-process Google account manager uses system browser, random loopback port, PKCE S256, state validation, offline refresh, user-info verification, Keychain persistence, grant revocation, and trusted build-time publisher configuration | Controlled integration verifies authorize/exchange/account/refresh/revoke; strict build-input and leak audits pass; the packaged 0.3.1 app completed real consent, exchanged the code, verified the account, reported both Gmail and Drive connected, and remained connected after a full restart | Added ignored local developer setup, trusted-main-only Desktop public-client metadata, credential-free renderer boundary, shared account consent/status, sanitized production/testing errors, and retained OAuth failure state across refresh; fixed the real token exchange by supplying the registered Desktop client metadata | Live refresh expiry and revoke were not forced because that would require waiting for expiry or remove the user's working grant; both paths are covered against controlled official-protocol responses |
| Gmail connector | 🟡 Partially Working | Thirteen tools cover search, message/thread/text-attachment reads, labels, drafts, send/reply, read state, archive, label changes, opaque attachment download, and user-selected native save | Request/auth/normalization/MIME/header-injection/cancellation/network/policy tests pass; the packaged 0.4.0 app used the persisted real account for an automatic bounded labels read, rendered exact details for a proposed send, and verified Cancel caused no send | Send/reply are communications: Ask and Approve-for-me request approval, while Full allows only ordinary non-critical calls; bulk/credential input and hard boundaries always ask. Gmail content remains bounded/untrusted and binary transfers stay opaque | Live mailbox mutations were intentionally not performed without disposable messages and per-action confirmation |
| Google Drive connector | 🟡 Partially Working | Fifteen tools cover search/recent/folder lists, metadata, text reads, Docs/Sheets/Slides export, opaque download/native save, text or transfer upload, folder create, rename, move, copy, trash, and restore | Request/auth/query/export/upload/invalid-UTF-8/network/cancellation/schema/policy tests pass; the packaged 0.3.1 app used the persisted real account for a no-result Drive search and received a valid API result without reading filenames/content | Binary bytes are refused for AI context; transfers preserve exact bytes without revealing local paths; all changes use main-process permission enforcement and required confirmations | Live Drive writes/organization were intentionally not performed without disposable files and per-action confirmation |
| Google Workspace transfers | ✅ Working & Verified | Private user-data transfer store moves Gmail/Drive binary content by opaque UUID with one-hour expiry, 24 MiB limit, `0600` files, integrity checks, and app-quit cleanup | Unit tests cover creation, consumption, corruption, expiry, limits and cleanup; four Work-agent integration tests completed Gmail attachment→Drive, Drive file→Gmail draft, Gmail summary, and Drive analysis flows without exposing bytes/base64/paths to model messages | Replaced model-visible binary/base64 handoff with an opaque main-process capability; native local saves require a real macOS destination dialog | Live service execution remains part of the external Google OAuth/account block, not a claimed pass |
| Work connector result cards | ✅ Working & Verified | Tool activity can include bounded display-only Gmail, Drive, and file previews projected by the trusted Work agent and validated before persistence | Unit/renderer tests pass; dark and 960×600 light packaged smokes persisted and reloaded one Gmail and one Drive card, verified their content, found no internal IDs, and logged zero errors | Replaced generic success-only activity with provider-specific cards while excluding service IDs, URLs, transfer capabilities, local paths, raw bodies, and binary data | Live card population from a real Google account remains part of the external OAuth/account block |
| Work attachments | 🟡 Partially Working | Native picker/drop imports private copies with opaque IDs; bounded text/source context and persisted safe metadata | Unit tests cover mode `0600`, names, traversal/symlink/binary/size rejection, context bounds, removal, and cleanup; live Markdown attachment content reached Gemini without exposing its source path | Replaced persistent external-path capabilities with controlled private copies | PDF, Word, spreadsheet, and image extraction are not implemented |
| Repository import | ✅ Working & Verified | Open existing repository and clone entry points in Welcome, File, palette, and Source Control; inspection, progress, cancel, validation | Real normal-folder/open-repository transitions passed; local bare clone reported progress through completion and opened the exact repo; early cancellation produced cancelled state and no output; invalid URL failed without leaking raw output | Added sanitized origin metadata, HTTPS/SSH/local validation, real stderr progress parsing, sender-bound cancellation, and polished progress UI | Authenticated GitHub write testing remains externally blocked |
| Packaging | 🟡 Partially Working | Versioned x64/arm64 DMG and ZIP via electron-builder | All four 0.5.1 artifacts pass ZIP/DMG integrity, embedded version, original icon, executable/native-module architecture, and SHA-256 validation; rebuilt x64 directory app passes packaged core and Cursor audits after the first unsigned nested-helper provenance scan | Preserved the original `build/icon.icns` byte-for-byte, restored the development `node-pty` module to x86_64 after cross-packaging, generated release-specific SHA-256 sums, and recorded the repeatable cold unsigned-helper timeout instead of treating the warm retry as a signed release result | Apple Silicon launch, stable cold helper identity, and Apple signing/notarization remain externally blocked |
| macOS integration | 🟡 Partially Working | Menus, native and styled dialogs, notifications, Finder open/reveal, file associations, theme | Packaged launch, native folder/destination/Open/Save As/Open With/confirmation sheets, accessible styled text-input dialogs, exact Finder reveal, OS file/folder drop, window lifecycle, native dirty-close choices, all 11 application menus/48 expected items, and real System Events shortcuts passed; notification validation passed but the unsigned app cannot be delivered by macOS Notification Center | Fixed native Undo/Redo routing, Sonoma Inline AI shortcut fallback, unreliable Finder selection, and Electron's unsupported `window.prompt()` calls | Native notification presentation remains blocked only by the missing Developer ID signature |
| File explorer / workspace | ✅ Working & Verified | Explorer UI and main-process open/read/write/create/rename/move/duplicate/trash/reveal/watch/project APIs | Packaged workflows passed actual context-menu New Folder/New File/Rename/Duplicate/Trash with styled/native dialogs and exact Unicode/space/period paths, native folder Open/Cancel/restoration, New Project, nested create, search/replace, native file Open/Save As, exact Finder reveal, Open With/TextEdit, external file/folder drop, Explorer drag-move, binary/boundary rejection, delayed autosave, external lifecycle, recent-folder restart/reopen, moved-workspace recovery, and permission-denied saves | Replaced unreliable Finder reveal with macOS `open -R`; replaced unsupported project/file/rename prompting; context action rejections now reach visible error reporting | None in the tested workspace lifecycle |
| Monaco editor | ✅ Working & Verified | Bundled Monaco, tabs, dirty state, save/autosave, language mapping, diff editor | Packaged workflow passed three tabs, switching/closing, system clipboard copy/paste with restoration, native Undo/Redo/Save/Save As/Find, inline Replace All, exact disk bytes, delayed autosave, unsaved-tab cancel/discard, native dirty-window Save All/Discard/Cancel, conflict preservation, indentation, brackets, line/syntax/folding/minimap rendering, language state, panel relayout, and zero renderer errors | Fixed Monaco 0.56 provider disposal and routed native Undo/Redo to the Monaco text-model history | None in the tested editor workflow |
| Workspace search | ✅ Working & Verified | ripgrep plus fallback, include/exclude options, replace UI | Real packaged UI search found two exact disposable-file results; a 1,203-file non-Git workspace returned only the one valid match in 47 ms while honoring `.gitignore`, `.omnicodeignore`, default folders, include, and exclude globs; Replace All changed both disk files | Fixed Replace All notice; fixed ripgrep glob precedence and non-repository ignore handling | None in the tested bounded workspace |
| Terminal | ✅ Working & Verified | xterm UI, multiple sessions, split, rename, restart/kill/clear/search, real `node-pty` backend | Packaged zsh workflow passed real cwd, identity, environment, Ctrl+C, history replay, search/highlight, clear, resize, tab switching, new/split/rename/restart/kill, system clipboard copy/paste, 1,520-row scrollback, advertised shortcuts, UI mount, and outside-workspace rejection; switching workspaces replaced the old PTY and the new shell printed the exact new `pwd` before restoration | Replaced unsupported terminal-name prompting; Run-session exit reporting and idle cursor repaint were repaired separately | None in the tested terminal and workspace-switch lifecycle |
| Shell environment | ✅ Working & Verified | Login-shell environment recovery and Intel/Apple Homebrew PATH additions | Packaged PTY found Node and Git and contained both `/usr/local/bin` and `/opt/homebrew/bin`; unit tests pass | Earlier PATH recovery fixes are present | Recheck on Apple Silicon hardware when available |
| Run / compile | ✅ Working & Verified | Run recipes for scripts, compilers, package projects, and validated `.omnicode/settings.json` custom configuration | Real packaged runs passed Python, Node, C, C++, Swift, Java, npm scripts, and a custom preRun/build/run/postRun chain with a space-containing cwd and inherited environment; deliberate failures were nonzero and useful; Run UI and the captured Run Log showed real stdout and exit code | Added explicit `[Process exited with code N]` reporting; relabeled the output mirror from misleading `Debug` to accurate `Run Log` | Rust and Go execution are separately blocked because those runtimes are absent on this host |
| Local development server | ✅ Working & Verified | Static server, live-reload injection, npm-script server detection/start/stop/restart | Rebuilt packaged app served real HTML/CSS/JS and nested assets, emitted reload SSE after a write, opened the default browser, reloaded the real Chrome page to changed HTML, handled conflict/auto ports, restarted, stopped, released ports, and reflected both static/package state in the Run UI with zero renderer errors | Package servers now wait for a reachable localhost port; premature process exit and startup timeout are real failures; explicit port conflicts are readable | None in the tested static/npm server lifecycle |
| npm project support | ✅ Working & Verified | Manifest script detection, npm/pnpm/yarn/bun selection, Run-view script execution, and development-server lifecycle | Disposable dependency-free npm project was detected; real `npm run verify` passed through the packaged Run UI with stdout/exit code; real npm server start/restart/stop passed; failing script exited 7 and was rejected | Removed fake package-server success immediately after spawn; no packages were installed automatically | Other package managers remain unit-covered but unavailable for real host execution |
| Git | ✅ Working & Verified | Status/diff/stage/unstage/commit/fetch/pull/push, branch UI, clone/init/import backend | Rebuilt packaged app passed UI init/diff/stage/unstage/commit and create/switch/delete branch; real open-repository and normal-folder paths were distinguished; local bare clones completed and cancelled with accurate progress/final state; staged rename metadata and bare-remote fetch/pull/push passed | Fixed rename/copy parsing, URL/error redaction, dialogs, repository inspection, progress parsing, and sender-bound cancellation | Merge has no claimed UI |
| GitHub | 🔵 Blocked / External Requirement | No GitHub-specific API; generic Git supports HTTPS/SSH URLs and existing system credentials | Public HTTPS clone of GitHub's `octocat/Hello-World` succeeded with system Git; local manager clone/remote lifecycle passed | Confirmed GitHub CLI is not installed and this repository has no remote; Git errors now redact common credential forms | Authenticated HTTPS/SSH fetch/pull/push require user credentials and a safe writable repository |
| Runtime detection | ✅ Working & Verified | 33 allowlisted tool probes and clean installed/missing states | Packaged detection matched independent host lookup for Git, Homebrew, CLT, Clang/Clang++, Swift, Python, Node/npm, Java/javac, .NET, Rust/Cargo, Go, Ruby, PHP, and Ollama | Fresh-Mac Apple shim prompting was fixed before baseline | Apple Silicon recheck remains hardware-blocked |
| Runtime installers | 🟡 Partially Working | Setup install/progress/cancel/retry; Homebrew formula and native installer handoff paths | Installer/detection tests and URL checks pass; the packaged Setup `Install Go` control opened the real native sheet and Cancel started no process/progress or false state; an invalid ID was rejected before a dialog; a separate real Go attempt reached Homebrew and surfaced its exact non-writable-directory failure without claiming success | Secure fixed allowlist, actual Setup control, native confirmation, progress, failure state, cancellation, and workbench return are verified | Successful installation of a missing system runtime remains blocked by the host's Homebrew ownership/interactive system requirements |
| Hardware detection | ✅ Working & Verified | CPU, architecture, RAM, macOS, Metal/GPU and model recommendations | Rebuilt packaged app matches Sonoma 14.8.9 x64 host: Intel i5, 4 logical/2 physical cores, 8 GiB, UHD 617, Metal 3, non-unified memory | Fixed Metal-family token parsing and stopped display panels such as `Color LCD` being classified as GPUs | Apple Silicon semantics remain covered by unit tests; hardware launch unavailable |
| macOS Keychain | ✅ Working & Verified | Save/has/delete IPC and per-provider Settings controls | Isolated native save/replace/reopen/delete passed for all providers; packaged Settings saved a temporary key, a full app restart retained and used it, and Settings removed it without touching the user's Google key | Empty-write and delete-stderr bugs fixed in 0.1.1; packaged restart regression audit added | None on the tested Intel Sonoma host |
| OpenAI adapter | 🟡 Partially Working | Chat Completions HTTPS adapter, bearer auth, model field, error parsing | Mock request/auth/response tests pass; packaged app retrieved a deliberately invalid credential after restart, sent it to OpenAI, and surfaced an accurate redacted authentication failure | Key retrieval and empty/refusal handling fixed in 0.1.1; connection-test endpoint added | Successful live request blocked until a valid key is configured |
| Anthropic adapter | 🟡 Partially Working | Messages API adapter and system-message separation | Mock request/auth/response tests pass | Empty/blocked replies now surface | Real request blocked unless configured key is available; token budget review |
| Google Gemini adapter | ✅ Working & Verified | GenerateContent adapter, API-key header, response parser | Rebuilt packaged app retrieved the existing Keychain credential, authenticated against Google's models API, sent the exact minimal live prompt, and received the exact expected response with zero renderer errors | Key persistence/thought filtering fixed in 0.1.1; live connection validation added | Account/model availability can still produce honest transient provider errors |
| Provider status UI | ✅ Working & Verified | Settings separates stored/not-tested, connected, authentication-failed, unavailable, and not-configured states; Save triggers a real test and each saved provider has Test Connection | Exact provider endpoints/headers and failures pass unit tests; packaged invalid-key save/test/restart/delete and valid-Gemini connection passed | Removed the fake implication that stored credentials are connected; added real lightweight connection tests | OpenAI and Claude success states remain externally blocked without configured valid keys |
| Ollama detection | ✅ Working & Verified | CLI/app detection plus loopback API status distinguishes absent, installed-but-stopped, and available | Current host has no CLI/app and refused port 11434; rebuilt packaged API returned installed=false/available=false and the Models UI said `Ollama not installed` with zero renderer errors | App-bundle detection/start fixed in 0.1.1; raw offline chat error replaced with an actionable installed/service diagnosis | Real installed/stopped and available states remain unit-covered but require Ollama to reproduce on this host |
| Ollama model library | 🟡 Partially Working | Catalog, installed/running list, pull/cancel/delete/load/unload/default UI | Parser/lifecycle tests pass | Setup downloads added in 0.1.1 | Real service/model operations require available Ollama instance |
| Local AI inference | 🔵 Blocked / External Requirement | Ollama `/api/chat` request path wired into AI chat | Packaged request correctly failed without claiming success and now explained that Ollama is not installed; HTTP/model failures and offline installed/absent messages have regression tests | Replaced low-level `TypeError: fetch failed` with host-state guidance while preserving real HTTP diagnostics | BLOCKED — OLLAMA INSTALLATION AND AN INSTALLED MODEL REQUIRED for real inference |
| Code AI chat | ✅ Working & Verified | Dynamic provider/model selectors, messages, safe Markdown/code rendering, all context attachments, clear chat, errors | UI-state and Markdown tests pass; live Gemini used the selected account-visible 3.7 model and returned the exact expected Code marker; prior packaged context/Markdown/error audits remain valid | Replaced the raw cloud model field with the shared catalog selector and refresh; provider/model synchronization remains validated | Code Chat streaming/stop and restart persistence are not implemented; Work Mode provides those behaviors |
| Workspace indexer | ✅ Working & Verified | Bounded text indexing, ranking, ignore rules, relevant-file retrieval, and watcher-driven refresh | Rebuilt package indexed 1,203 files in 1.53 s while the renderer remained responsive; smaller lifecycle tests refreshed update/create/rename/delete and excluded sensitive/ignored/binary data | Added debounced watcher reindexing; fixed unrelated metadata matches and concurrent same-workspace indexing returning stale status | None within the tested 1,203-file fixture and documented bounds |
| AI context controls | ✅ Working & Verified | Current/open/picker-selected files, workspace retrieval, code selection, terminal, problems, Git | Packaged consent listed every exact path/category and nonzero size; a native-picked file plus all six checkbox categories reached one real Gemini request, which returned the expected seven-context marker; separate live multi-file workspace retrieval derived undisclosed values and `/etc/hosts` was denied | Workspace context freshness/relevance fixed; main process authorizes every attached path | None in the implemented context controls |
| Inline AI edit | 🟡 Partially Working | Selected text request, generated replacement, Monaco diff, accept/reject | Real packaged native shortcut opened the selected-code prompt; backend AI and renderer wiring inspected | Added a capture-phase Sonoma fallback so advertised `⌘I` works when Electron drops the custom menu accelerator | Real/model-backed proposal and save/undo behavior remains blocked by unavailable Ollama |
| Code Agent / Glasses Mode | ✅ Working & Verified | Main-process provider tool loop; registered workspace, external-file, PTY, build/test, Git, server, runtime, Code Browser, and allowlisted macOS-app tools; provider-neutral dynamic plan/reasoning summaries; independent Ask/Auto/Full, Standard/Glasses, and focus controls; pause/resume/Take Over/Modify Plan/Skip Step/stop; private activity | Automated lifecycle, plan persistence/redaction, initial/change/progress/decision state, intervention, stale-call cancellation, policy, real temporary-file/diff/undo, terminal ownership, browser/localhost/search, app allowlist, timeline, failure/final reporting, and injection-boundary tests pass. A packaged 0.5.1 controlled local model audit exercised five provider turns, both intervention controls, runtime detection, dynamic 2/2 progress, Standard/Glasses, full reporting, no overflow, and zero renderer errors. The earlier packaged Gemini task completed a real diff-backed write/read and an honest later 429 | Replaced opaque progress with a bounded `agent.update-plan` metadata tool and compact status/plan/final-report UI; course changes pause before stale calls and remain separate from Permission Manager authority. Existing real Agent tool architecture and Google model-default repair are preserved | Arbitrary external-app click/type/screen observation is not claimed; it needs a separately audited native Accessibility ComputerTool. Current Google quota may block additional live provider runs |
| Diff review / undo | ✅ Working & Verified | File/hunk accept/reject/all/undo/discard with event updates and transactional filesystem writes | Rebuilt packaged app staged real modify/create/delete changes without disk writes, accepted all, matched disk, undid the snapshot, rejected another proposal without changes, denied traversal, and repeated the accept/undo path with a live Agent-created proposal; zero renderer errors | No defect found; new packaged transaction/security audits added | None in tested flows |
| Settings | ✅ Working & Verified | Theme/autosave/context/autocomplete preferences, Work Mode and Connected Apps sections, global and per-connector Work action modes, shared model dropdowns, validated workspace JSON, Keychain controls | Packaged UI persistence audits remain valid; 0.4.0 live UI verified all three action modes, Full warning/cancel, a Gmail override, restart-safe main-process storage, and restoration; provider/connector/model checks remain covered | Added strict normalization, real connector management, shared model catalog selection, and separated Work action permission policy from AI context permissions | None in the tested settings paths; unavailable external connectors remain honestly gated |
| Themes and layout | ✅ Working & Verified | Dark/light/system styles and bounded resizable panels | Rebuilt packaged app switched all three themes; muted/accent/warning contrast was at least 4.5:1 in light and dark modes; focus outline and disabled opacity passed; Explorer/AI/panel resized to every min/max bound and all visibility controls plus terminal toggle worked | Raised low-contrast theme tokens to verified ratios | Layout persistence is accurately documented as not implemented, not presented as working |
| Security boundaries | ✅ Working & Verified | Sandboxed renderer, trusted IPC sender checks, workspace path authorization, sensitive-file exclusions, diff canonicalization, and main-process Agent command/intervention policy | Packaged tests deny outside AI attachments, diff traversal, wrong Agent workspace, `sudo`, and stale post-intervention calls; diff acceptance revalidates paths; focused tests block privileged/destructive/Keychain/nested-shell commands; the native command sheet canceled without side effects and accepted one safe workspace command; final 0.5.1 scans covered 238 repository and 30 isolated-profile files plus the empty plan workspace, local storage, and visible diagnostics with zero unexpected credential-shaped values | Agent policy, native confirmation, plan mutation, and action-boundary stale-call cancellation are enforced in the main process; synthetic redaction fixtures no longer create repository-scan false positives | None in the implemented/tested boundaries; continue security review as code changes |
| Logging / diagnostics | ✅ Working & Verified | Central main-process JSON-lines diagnostics record lifecycle and failed IPC operation metadata; logs rotate at 512 KiB and use mode `0600` | Five unit tests verify structure, categorization, concurrency, rotation, symlink refusal, and provider/token/header redaction; the rebuilt package recorded a real workspace denial as `workspace`/`permission`, preserved the UI error, omitted its synthetic sensitive argument, and recorded startup | Added the persistent logger at the trusted IPC boundary without logging IPC arguments, request bodies, file contents, stacks, credentials, or authorization headers | None in the implemented diagnostic scope |
| Performance | ✅ Working & Verified | Bounded indexing/search/output and explicit PTY/server cleanup paths | Packaged 1,203-file soak: workspace open 415 ms, index 1.53 s, search 47 ms, responsive renderer, 8 PTYs/3 servers cleaned; refreshed three-launch cycle used 392–441 MiB and 1.4–2.6% settled CPU with zero renderer/fatal errors and complete cleanup | Fixed search filtering, duplicate index races, xterm cursor repaint load, and fresh-profile soak coverage | Build bundling remains slow on this Intel host but packaged runtime bounds pass |

## Explicitly absent capabilities

These are not failures unless existing UI or documentation claims otherwise:

- ⚪ Tauri/Rust backend
- ⚪ VS Code extension-host compatibility
- ⚪ Native debugger adapter or language-server client
- ⚪ Git merge UI and GitHub-specific API authentication
- ⚪ Code Mode Chat streaming/stop and Code-chat persistence (implemented in Work Mode only)
- ⚪ Window and panel-layout persistence
- 🟡 Structured pointer/keyboard control foundation for allowlisted external
  apps; packaged TextEdit input is live-verified, while semantic AX/screen
  targeting and broader adversarial application coverage remain incomplete
- ⚪ Native post-quit Omni activation helper (resident shortcut, restricted
  overlay, tray, and packaged background login launch are implemented)
- 🟡 Omni push-to-talk STT is implemented but real capture is blocked on this
  host by the missing `en-US` on-device Speech asset; local “Hey Omni” wake
  activation is not implemented (macOS TTS is implemented)
- ⚪ Omni helper-owned stop event tap and semantic AX/screen understanding (the
  renderer-independent main-process `⌘⇧Esc` stop is implemented)
